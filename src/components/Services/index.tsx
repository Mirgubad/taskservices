import styles from "./services.module.css"

const Services = () => {
    return (
        <div className={styles.title__wrapper}>
            <a className={styles.title}>Services</a>
            <span className={styles.title__count}>0</span>
        </div>
    )
}

export default Services
